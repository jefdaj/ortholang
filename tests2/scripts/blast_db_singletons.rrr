# TODO move somewhere else?

nums = [1,2,3]
strs = ["1", "2", "3"]

genes10 = load_faa "Mycoplasma_genitalium_M2321_10genes.faa"
mgen    = gbk_to_faa (load_gbk "Mycoplasma_genitalium_M2321.gbk")
dbs = makeblastdb_prot_each [genes10, mgen]

# should be [[1, 1, 1], [1, 1, 1], [1, 1]]
# (as opposed to some list error)
result = [length_each (singletons nums),
          length_each (singletons strs),
          length_each (singletons dbs)]
