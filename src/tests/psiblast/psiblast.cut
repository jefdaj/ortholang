# the simplest psiblast function to use
# takes a query and subject, both amino acid fasta files
# makes a blast datbase from the subject fasta
# trains a pssm of the query against the subject
# runs a final search and returns the hit table

query   = load_faa "data/7942gene.faa"
pcc7942 = load_faa "data/7942genes.faa"
hits    = psiblast 1e-10 query pcc7942
result  = hits
