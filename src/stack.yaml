# TODO load all the nix dependencies in here, including the scripts?

resolver: lts-8.6
packages:
- '.'
extra-deps:
- Command-0.0.7
- lock-file-0.5.0.2 # TODO remove if using concurrent-extra
- tagged-exception-core-2.2.0.0
- concurrent-extra-0.7.0.11
- posix-escape-0.1
- temporary-1.2.1.1
flags: {}
extra-package-dbs: []
nix:
  enable: true
  pure: false
