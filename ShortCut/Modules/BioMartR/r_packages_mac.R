#!/usr/bin/env Rscript

require(biomartr); packageVersion('biomartr')
require(dplyr); packageVersion('dplyr')
require(ggplot2); packageVersion('ggplot2')
require(readr); packageVersion('readr')
require(tidyr); packageVersion('tidyr')
require(UpSetR); packageVersion('UpSetR')
require(VennDiagram); packageVersion('VennDiagram')
require(data.table); packageVersion('data.table')
require(futile.logger); packageVersion('futile.logger')
