# TODO does this show the same bug as load:fna.ol and load:gbk.ol?

single = "examples/sequences/Mycoplasma_genitalium_M2321_single.fna"
mapped = ["examples/sequences/Mycoplasma_genitalium_M2321_single.fna"]
result = [single] | mapped
