# Simplest example of including one script in another. The result variable
# should be imported here too. This is a little useless on its own, because
# it's the same as loading list_exprs.ol directly.

# Result should be 1 (same as list_exprs.ol)

include "list_exprs.ol"
