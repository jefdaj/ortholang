# replace_each was written first so i know it works
# this tests whether the single replace function works too and matches it

ind = 1
dep = 1.2e3 * ind

dep1 = replace dep ind 1
dep2 = replace dep ind 2
dep3 = replace dep ind 3
dep4 = replace dep ind 4
dep5 = replace dep ind 5

# these should give the same results, but via different tmpfiles
depsSeparate = [dep1, dep2, dep3, dep4, dep5]
depsTogether = replace_each dep ind [1,2,3,4,5]

# should be [[], []]
# TODO think about a better way to assert equality
result = [depsSeparate ~ depsTogether, depsTogether ~ depsSeparate]
