orthogroup_containing : og str -> str.list
where
  og = any orthogroup (ofr, spr, or gcr)
  str = string

Given one gene ID, list others in the same orthogroup.
Returns an empty list if there aren't any.