maga = load_faa "data/Mycoplasma_agalactiae_small.faa"
db1 = mmseqs_createdb_all [maga]
db2 = mmseqs_createdb_all (load_faa_each (glob_files "data/Mycoplasma_*.fa*"))

# error here before actually running createdb?
# can mmseqs2 not search a fa against itself? nah probably not the problem right?
hits1 = mmseqs_search_db 1.0e-30 maga db1

# hits2 = mmseqs_search_db 1.0e-30 maga db2
# result = length_each [hits1, hits2]
result = length_each [hits1]
