[ 
    ( Var ( RepID Nothing ) "my_species" 
    , Fun fna Nothing [] "gbk_to_fna" 
        [ Lit str "cds" 
        , Fun gbk Nothing [] "load_gbk" [ Lit str "examples/sequences/PCC_6803_genome.gbff" ]
        ] 
    ) 
, 
    ( Var ( RepID Nothing ) "swissprot" 
    , Fun faa.blastdb Nothing [] "blastdbget_faa" [ Lit str "swissprot" ]
    ) 
, 
    ( Var ( RepID Nothing ) "blast_hits" 
    , Fun bht Nothing 
        [ Var ( RepID Nothing ) "my_species" 
        , Var ( RepID Nothing ) "swissprot" 
        ] "blastx_db" 
        [ Lit num "1.0e-20" 
        , Ref fna Nothing [] 
            ( Var ( RepID Nothing ) "my_species" )
        , Ref faa.blastdb Nothing [] 
            ( Var ( RepID Nothing ) "swissprot" )
        ] 
    ) 
, 
    ( Var ( RepID Nothing ) "genes_with_hits" 
    , Fun str.list Nothing 
        [ Var ( RepID Nothing ) "blast_hits" 
        , Var ( RepID Nothing ) "my_species" 
        , Var ( RepID Nothing ) "swissprot" 
        ] "extract_queries" 
        [ Ref bht Nothing 
            [ Var ( RepID Nothing ) "my_species" 
            , Var ( RepID Nothing ) "swissprot" 
            ] 
            ( Var ( RepID Nothing ) "blast_hits" )
        ]
    ) 
, 
    ( Var ( RepID Nothing ) "nr_seq_matches" 
    , Fun str.list Nothing 
        [ Var ( RepID Nothing ) "blast_hits" 
        , Var ( RepID Nothing ) "my_species" 
        , Var ( RepID Nothing ) "swissprot" 
        ] "extract_targets" 
        [ Ref bht Nothing 
            [ Var ( RepID Nothing ) "my_species" 
            , Var ( RepID Nothing ) "swissprot" 
            ] 
            ( Var ( RepID Nothing ) "blast_hits" )
        ]
    ) 
, 
    ( Var ( RepID Nothing ) "result" 
    , Lit str "nothing so far" 
    ) 
] 