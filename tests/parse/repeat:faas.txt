Script
    { sAssigns =
        [ Assign
            { aVar = Var ( RepID Nothing ) "faa_names"
            , aExpr = Fun str.list Nothing [] "glob_files" [ Lit str "examples/sequences/Mycoplasma_*.faa" ]
            }
        , Assign
            { aVar = Var ( RepID Nothing ) "use5"
            , aExpr = Fun str.list
                ( Just ( Seed 0 ) )
                [ Var ( RepID Nothing ) "faa_names" ] "sample"
                [ Lit num "5.0"
                , Ref str.list Nothing []
                    ( Var ( RepID Nothing ) "faa_names" )
                ]
            }
        , Assign
            { aVar = Var ( RepID Nothing ) "use5manytimes"
            , aExpr = Fun str.list.list Nothing
                [ Var ( RepID Nothing ) "use5"
                , Var ( RepID Nothing ) "faa_names"
                ] "repeat"
                [ Ref str.list
                    ( Just ( Seed 0 ) )
                    [ Var ( RepID Nothing ) "faa_names" ]
                    ( Var ( RepID Nothing ) "use5" )
                , Ref str.list Nothing []
                    ( Var ( RepID Nothing ) "faa_names" )
                , Lit num "100.0"
                ]
            }
        , Assign
            { aVar = Var ( RepID Nothing ) "result"
            , aExpr = Fun str.list Nothing
                [ Var ( RepID Nothing ) "use5manytimes"
                , Var ( RepID Nothing ) "use5"
                , Var ( RepID Nothing ) "faa_names"
                ] "all"
                [ Ref str.list.list Nothing
                    [ Var ( RepID Nothing ) "use5"
                    , Var ( RepID Nothing ) "faa_names"
                    ]
                    ( Var ( RepID Nothing ) "use5manytimes" )
                ]
            }
        ]
    , sResult = Just
        ( Fun str.list Nothing
            [ Var ( RepID Nothing ) "use5manytimes"
            , Var ( RepID Nothing ) "use5"
            , Var ( RepID Nothing ) "faa_names"
            ] "all"
            [ Ref str.list.list Nothing
                [ Var ( RepID Nothing ) "use5"
                , Var ( RepID Nothing ) "faa_names"
                ]
                ( Var ( RepID Nothing ) "use5manytimes" )
            ]
        )
    }